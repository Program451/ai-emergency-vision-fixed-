fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.4
python-multipart==0.0.9
opencv-python-headless==4.10.0.84
# БАГФИКС: torch не был закреплён — pip ставил новейший torch (2.6+), где
# изменили дефолт `weights_only=True` в torch.load. Из-за этого
# ultralytics==8.2.28 не мог загрузить ни свои веса (yolov8n.pt), ни
# custom_model.pt — падал с UnpicklingError на КАЖДОМ реальном инференсе.
# По факту, реальная ИИ-модель (не mock) вообще никогда не запускалась.
# Фиксируем пару версий, где этой проблемы нет.
torch==2.3.1
torchvision==0.18.1
ultralytics==8.2.28
numpy==1.26.4
httpx==0.27.0
python-dotenv==1.0.1
websockets==12.0
